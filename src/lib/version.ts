export const APP_VERSION = "v17";

export const APP_VERSION_CHANGES = [
  "fixed a bug where a low-confidence TMDB match could render the wrong show's poster/rating/synopsis — now shows the model's title with no poster instead of guessing",
];
